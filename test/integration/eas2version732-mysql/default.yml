---
- hosts: test-kitchen 
  become: True
  roles:

    # Note that the vm preparation role is not required by eas2 role
    # It's only used to provision docker for local tests, since in our docker we don't have eas2 user by default
    - role: kn.as2.vm.preparation
      update_etc_hosts: false

    - role: "kn.as2.eas2"
      ansible_python_interpreter: /usr/local/bin/python2.7
      db_name: dev_default
      db_password: Dk4Mrs0K
      db_host: dehamsl1410.int.kn
      db_user: as2data
      eas2_instance_name: "KITCHEN"
      message_id_hostname: "as2dev"
